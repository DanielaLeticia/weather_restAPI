# redis service
---
apiVersion: v1
kind: Service
metadata:
  name: weather-app-redis-service
spec:
  type: ClusterIP
  selector:
    app: weather-app
  ports:
  - name: weather-app
    port: 5000
    targetPort: 5000
